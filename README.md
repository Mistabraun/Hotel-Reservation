# Hotel Reservation
Hotel reservation website project

# Installation
Run `npm install` on VSCode cmd Terminal
Create a folder named `libraries` then transfer bootstrap and fontawesome to that folder.
